---
title: "HCLSM: Hierarchical Causal Latent State Machines for Object-Centric World Modeling"
title_zh: HCLSM：面向以对象为中心的世界建模的分层因果潜状态机
authors: "Jaber Jaber, Osama Jaber"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29090v1"
tags: ["query:worldmodel-robot"]
score: 10.0
evidence: 用于世界建模的分层因果潜状态机
tldr: 针对现有世界模型在物体解耦、因果结构和多尺度动力学方面的局限，本文提出HCLSM架构。该模型通过Slot Attention实现物体中心化分解，并构建了结合选择性状态空间模型（SSM）、稀疏Transformer和压缩Transformer的三层分层引擎，以捕捉从连续物理到抽象目标的动态。实验表明，该模型在机器人操作任务中表现优异，且推理效率显著提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29090v1/fig-001.webp\", \"caption\": \"Figure 2: Spatial broadcast decoder output. Top row: per-slot alpha heatmaps (ownership probability). Bottom row: slot overlays on input frame. Left: segmentation map (argmax over slots). Different colors indicate different slot assignments, showing emerging spatial decomposition.\", \"page\": 7, \"index\": 1, \"width\": 826, \"height\": 187}, {\"url\": \"assets/figures/arxiv/2603.29090v1/fig-002.webp\", \"caption\": \"Figure 3: Event detection across four episodes. Blue = event probability over time. Red dashed = detected event boundaries. The model learns to fire at moments of state transition (2–3 per episode).\", \"page\": 8, \"index\": 2, \"width\": 826, \"height\": 702}, {\"url\": \"assets/figures/arxiv/2603.29090v1/fig-003.webp\", \"caption\": \"Figure 1: HCLSM architecture. Five layers process video into structured world states. The spatial broadcast decoder (SBD, dashed) provides the reconstruction signal that drives slot specialization during Stage 1 training.\", \"page\": 4, \"index\": 3, \"width\": 531, \"height\": 548}, {\"url\": \"assets/figures/arxiv/2603.29090v1/fig-004.webp\", \"caption\": \"Figure 4: Slot state trajectories projected to 2D via PCA. Circles = start, squares = end. Different colors = different slots. Trajectories show structured temporal dynamics with slot-specific evolution paths.\", \"page\": 9, \"index\": 4, \"width\": 582, \"height\": 467}]"
motivation: 传统世界模型因采用扁平化表征而难以有效处理物体解耦、因果逻辑及多尺度时间动力学问题。
method: 采用槽位注意力进行空间分解，并结合SSM、Transformer及图神经网络构建分层因果动力学模型。
result: 在PushT基准上实现了0.008的预测均方误差，且自定义Triton内核带来了38倍的计算加速。
conclusion: HCLSM通过分层架构和物体中心化设计，显著增强了世界模型对复杂物理交互的建模能力。
---

## 摘要
从视频预测未来状态的世界模型仍受限于扁平的潜表示，这些表示将对象纠缠在一起，忽略了因果结构，并将时间动力学压缩为单一尺度。我们提出了 HCLSM，这是一种基于三个互连原则运行的世界模型架构：通过带有空间广播解码的槽注意力（slot attention）进行以对象为中心的分解；通过结合用于连续物理的选择性状态空间模型（SSM）、用于离散事件的稀疏 Transformer 以及用于抽象目标的压缩 Transformer 的三层引擎实现分层时间动力学；以及通过图神经网络交互模式进行因果结构学习。HCLSM 引入了一种两阶段训练协议，其中空间重建在动力学预测开始前强制槽特化。我们在 Open X-Embodiment 数据集的 PushT 机器人操纵基准上训练了一个拥有 6800 万参数的模型，实现了 0.008 的均方误差（MSE）下一状态预测损失，并表现出新兴的空间分解能力（SBD 损失：0.0075）和学习到的事件边界。用于 SSM 扫描的自定义 Triton 内核比顺序 PyTorch 实现了 38 倍的加速。整个系统包含分布在 51 个模块中的 8,478 行 Python 代码，并配有 171 个单元测试。代码：https://github.com/rightnow-ai/hclsm

## Abstract
World models that predict future states from video remain limited by flat latent representations that entangle objects, ignore causal structure, and collapse temporal dynamics into a single scale. We present HCLSM, a world model architecture that operates on three interconnected principles: object-centric decomposition via slot attention with spatial broadcast decoding, hierarchical temporal dynamics through a three-level engine combining selective state space models for continuous physics, sparse transformers for discrete events, and compressed transformers for abstract goals, and causal structure learning through graph neural network interaction patterns. HCLSM introduces a two-stage training protocol where spatial reconstruction forces slot specialization before dynamics prediction begins. We train a 68M-parameter model on the PushT robotic manipulation benchmark from the Open X-Embodiment dataset, achieving 0.008 MSE next-state prediction loss with emerging spatial decomposition (SBD loss: 0.0075) and learned event boundaries. A custom Triton kernel for the SSM scan delivers 38x speedup over sequential PyTorch. The full system spans 8,478 lines of Python across 51 modules with 171 unit tests. Code: https://github.com/rightnow-ai/hclsm

---

## 论文详细总结（自动生成）

以下是对论文《HCLSM: Hierarchical Causal Latent State Machines for Object-Centric World Modeling》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的世界模型（如 V-JEPA）主要依赖“扁平化”的潜表示，这导致物体纠缠、忽略因果结构，并将不同尺度的动力学（如连续物理运动与离散事件）压缩在单一尺度中。
*   **研究动机**：人类感知世界是结构化的（识别物体、追踪运动、推理因果）。作者认为，一个有效的智能体世界模型必须具备三个维度：**物体解耦**（独立表示实体）、**时间分层**（处理不同频率的动态）和**因果推理**（理解交互逻辑）。

### 2. 方法论：HCLSM 架构
HCLSM 由五个互连层组成，核心思想是“结构必须先于预测”：
*   **感知层 (Perception)**：使用 Vision Transformer (ViT) 将视频帧编码为补丁嵌入。
*   **物体分解层 (Object Decomposition)**：利用 **Slot Attention** 将场景分解为多个“槽位”（Slots）。引入 **空间广播解码器 (SBD)**，通过重建冻结的 ViT 特征（而非原始像素）来强制槽位专注于特定空间区域。
*   **分层动力学引擎 (Hierarchical Dynamics)**：
    *   **L0 级 (SSM)**：使用选择性状态空间模型（Selective SSM/Mamba）处理物体级的连续物理动态。
    *   **L1 级 (稀疏 Transformer)**：仅在检测到“事件边界”（如碰撞）时触发，处理离散事件。
    *   **L2 级 (压缩 Transformer)**：通过交叉注意力压缩事件序列，处理抽象目标和长期规划。
*   **因果推理层 (Causal Reasoning)**：通过图神经网络 (GNN) 学习物体间的交互，并利用 NOTEARS 算法施加有向无环图 (DAG) 约束。
*   **两阶段训练协议**：
    *   **阶段 1**：仅进行空间重建（SBD 损失），强制槽位实现空间特化（学会“什么是物体”）。
    *   **阶段 2**：激活 JEPA 风格的动力学预测损失，在已解耦的物体上学习预测未来（学会“物体做什么”）。

### 3. 实验设计
*   **数据集**：使用 Open X-Embodiment 生态系统中的 **PushT** 机器人操纵任务（206 个情节，约 2.5 万帧）。
*   **任务**：下一帧潜状态预测。
*   **对比实验**：主要对比了“完整 HCLSM（两阶段训练）”与“无 SBD 的 HCLSM（扁平化训练）”。
*   **评估指标**：预测均方误差 (MSE)、SBD 重建损失、多样性损失、事件检测准确性及推理速度。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA H100 80GB GPU** 进行训练。
*   **训练细节**：
    *   模型参数量：68M（Small 配置）。
    *   训练时长：约 6 小时（5 万步迭代）。
    *   精度：bfloat16 混合精度。
    *   工程优化：开发了自定义 **Triton 内核** 用于 SSM 扫描，比原生 PyTorch 实现快 **38 倍**。

### 5. 实验数量与充分性
*   **实验规模**：论文主要针对 PushT 单一场景进行了深入实验。
*   **消融实验**：验证了两阶段训练的必要性，证明了如果没有第一阶段的重建引导，模型会退化为扁平的分布式表示。
*   **充分性评价**：实验在工程实现上非常扎实（包含 171 个单元测试），但在**泛化性验证上略显不足**。目前仅在单一机器人任务上验证，且由于数值稳定性问题（NaN 梯度），更大规模的 Base (262M) 和 Large (3B) 模型尚未完成完整训练。

### 6. 主要结论与发现
*   **结构化优势**：HCLSM 在 PushT 上实现了 0.008 的预测 MSE，并成功涌现出空间分解能力。
*   **训练顺序至关重要**：直接训练动力学预测会导致模型走捷径（不进行物体分解），必须先通过重建任务强制物体特化。
*   **分层有效性**：模型能够自动识别状态转换瞬间（事件边界），并生成结构化的物体运动轨迹。

### 7. 优点与亮点
*   **架构创新**：首次将 Slot Attention、分层 SSM/Transformer 和因果 GNN 统一在一个可微架构中。
*   **工程卓越**：提供了高性能的 Triton 内核优化，显著降低了物体级建模的计算开销。
*   **开源贡献**：发布了完整的代码库、训练基础设施和评估套件，具有极高的透明度。

### 8. 不足与局限
*   **数值稳定性**：在 bf16 精度下极易出现梯度爆炸（NaN），导致 50% 的训练任务失败，且难以扩展到大参数量模型。
*   **槽位管理**：槽位数量固定（32 个），无法根据场景复杂度动态增减，导致简单场景下物体被过度分割。
*   **因果学习失效**：显式因果邻接矩阵在实验中未能学习到有效边缘（权重趋于零），目前的因果性主要体现在隐式的 GNN 交互中。
*   **场景单一**：尚未在多物体、复杂交互的真实世界大规模数据集上验证。

（完）
