---
title: Enhancing Policy Learning with World-Action Model
title_zh: 利用世界-动作模型增强策略学习
authors: "Yuci Han, Alper Yilmaz"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28955v1"
tags: ["query:worldmodel-robot"]
score: 10.0
evidence: 用于策略学习的动作正则化世界模型
tldr: 本文提出世界-动作模型（WAM），旨在解决传统世界模型仅通过图像预测训练而忽略动作相关结构的问题。WAM在DreamerV2基础上引入逆动力学目标，通过预测潜在状态转移间的动作来正则化表征学习。在CALVIN操作任务中，该模型显著提升了行为克隆的初始性能，并在随后的强化学习微调中以更少的训练步数实现了更高的成功率，证明了动作感知表征对策略学习的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28955v1/fig-001.webp\", \"caption\": \"TABLE IV: Diffusion policy success rates (%) on 8 CALVIN tasks after 800 iterations of model-based PPO fine-tuning. DPPO columns report environment steps required to match DiWA performance (lower = more sample-efficient).\", \"page\": 6, \"index\": 1, \"width\": 1052, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2603.28955v1/fig-002.webp\", \"caption\": \"Fig. 4: Behavioral cloning evaluation curves across all eight CALVIN tasks. WAM (orange) consistently reaches higher success rates than DiWA (blue) and converges faster on most tasks.\", \"page\": 6, \"index\": 2, \"width\": 1038, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2603.28955v1/fig-003.webp\", \"caption\": \"Fig. 1: (a) Standard world models predict only future observations, treating actions solely as conditioning inputs. (b) Our World-Action Model adds an inverse dynamics head that jointly predicts observations and actions during training. (c) The action-aware world model serves as a learned simulator for offline policy fine-tuning via PPO.\", \"page\": 2, \"index\": 3, \"width\": 1052, \"height\": 427}, {\"url\": \"assets/figures/arxiv/2603.28955v1/fig-004.webp\", \"caption\": \"Fig. 2: WAM architecture. Observations xt are encoded and produce posterior zt, which is regularized toward the prior ẑt via KL divergence. The inverse dynamics head predicts actions ât from consecutive encoder embeddings, cascading action-aware structure through the posterior to the prior. The decoder reconstructs observations x̂t and a reward estimator provides task-completion signals for policy fine-tuning.\", \"page\": 3, \"index\": 4, \"width\": 527, \"height\": 489}, {\"url\": \"assets/figures/arxiv/2603.28955v1/fig-005.webp\", \"caption\": \"Fig. 3: Qualitative comparison of imagined rollouts on the CALVIN benchmark. We visualize predicted frames at selected timesteps from both static and gripper cameras. Compared to DreamerV2, our WAM produces more realistic future state predictions across the entire rollout horizon.\", \"page\": 4, \"index\": 5, \"width\": 1052, \"height\": 779}, {\"url\": \"assets/figures/arxiv/2603.28955v1/fig-006.webp\", \"caption\": \"TABLE III: Behavioral Cloning (BC) success rates (%) on 8 CALVIN manipulation tasks. Both methods use identical diffusion policy architectures and training procedures; the only difference is the world model used for feature extraction.\", \"page\": 5, \"index\": 6, \"width\": 468, \"height\": 281}]"
motivation: 传统世界模型主要依赖视觉预测，往往无法充分捕捉对下游控制任务至关重要的动作相关潜在结构。
method: 在DreamerV2框架中加入逆动力学目标，要求模型根据潜在状态的转换来预测对应的动作，从而强化表征的动作相关性。
result: "在CALVIN基准测试中，WAM将行为克隆成功率从59.4%提升至71.2%，PPO微调后成功率达92.8%，且训练效率提升了8.7倍。"
conclusion: 通过引入动作正则化，世界模型能够学习到更利于策略优化的表征，显著增强了复杂机器人操作任务的学习性能。
---

## 摘要
本文提出了世界-动作模型（World-Action Model, WAM），这是一种动作正则化的世界模型，能够对未来的视觉观测以及驱动状态转移的动作进行联合推理。与仅通过图像预测进行训练的传统世界模型不同，WAM 在 DreamerV2 中引入了逆动力学目标，通过潜在状态转移来预测动作，从而鼓励学习到的表示捕捉对下游控制至关重要的动作相关结构。我们在 CALVIN 基准测试的八个操纵任务中评估了 WAM 在增强策略学习方面的表现。我们首先在世界模型潜在空间上通过行为克隆预训练扩散策略，然后在冻结的世界模型内部使用基于模型的 PPO 对其进行微调。在不修改策略架构或训练程序的情况下，相比 DreamerV2 和 DiWA 基准，WAM 将平均行为克隆成功率从 59.4% 提高到了 71.2%。经过 PPO 微调后，WAM 实现了 92.8% 的平均成功率，而基准模型为 79.8%，其中两个任务达到了 100% 的成功率，且训练步数减少了 8.7 倍。

## Abstract
This paper presents the World-Action Model (WAM), an action-regularized world model that jointly reasons over future visual observations and the actions that drive state transitions. Unlike conventional world models trained solely via image prediction, WAM incorporates an inverse dynamics objective into DreamerV2 that predicts actions from latent state transitions, encouraging the learned representations to capture action-relevant structure critical for downstream control. We evaluate WAM on enhancing policy learning across eight manipulation tasks from the CALVIN benchmark. We first pretrain a diffusion policy via behavioral cloning on world model latents, then refine it with model-based PPO inside the frozen world model. Without modifying the policy architecture or training procedure, WAM improves average behavioral cloning success from 59.4% to 71.2% over DreamerV2 and DiWA baselines. After PPO fine-tuning, WAM achieves 92.8% average success versus 79.8% for the baseline, with two tasks reaching 100%, using 8.7x fewer training steps.

---

## 论文详细总结（自动生成）

以下是对论文《Enhancing Policy Learning with World-Action Model (WAM)》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机与背景）
*   **核心问题**：传统的世界模型（如 Dreamer 系列）主要通过**视觉预测（图像重建）**来学习环境动力学，动作仅作为条件输入。这种“非对称”训练导致学习到的潜在表示（Latent Representations）虽然能生成好看的图像，但未必包含对下游控制任务至关重要的**动作相关结构**。
*   **研究背景**：在机器人操纵任务中，策略学习（如行为克隆或强化学习）高度依赖于世界模型提供的特征质量。如果特征中缺乏对“动作如何改变状态”的显式建模，策略学习的效率和最终性能都会受限。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：引入**逆动力学（Inverse Dynamics）正则化**。要求模型不仅能预测未来图像，还能根据连续的编码器嵌入（Embeddings）预测出导致该状态转移的动作。
*   **关键技术细节**：
    *   **架构基础**：基于 DreamerV2 的 RSSM（循环状态空间模型）架构。
    *   **逆动力学头（Action Head）**：在编码器层添加一个三层 MLP，输入连续两个时刻的编码 $e_t$ 和 $e_{t+1}$，预测动作 $\hat{a}_t$。
    *   **级联效应（Cascading Effect）**：这是 WAM 的设计精髓。通过在编码器级别进行动作正则化，动作感知信息会流向后验分布 $z_t$，再通过 KL 散度损失传播到先验分布 $\hat{z}_t$。这意味着即使在没有真实观测的“想象”阶段，模型生成的潜在状态也带有动作敏感性。
    *   **损失函数**：$L_{WAM} = \lambda_{KL} L_{KL} + \lambda_{img} L_{recon} + \lambda_{act} L_{action}$。其中 $L_{action}$ 采用 L1 回归损失。
*   **策略学习流程**：
    1.  **预训练**：在冻结的 WAM 潜在空间上，利用专家数据进行扩散策略（Diffusion Policy）的行为克隆（BC）。
    2.  **微调**：在 WAM 内部（作为模拟器），利用 DPPO（扩散策略近端策略优化）进行离线强化学习微调。

### 3. 实验设计
*   **数据集与场景**：使用 **CALVIN 操纵基准测试**。这是一个复杂的桌面机器人操纵环境，包含 7 自由度的 Franka 手臂和 6 小时的遥操作数据。
*   **任务设置**：评估了 8 个具体的操纵任务（如开关抽屉、移动滑块、开关灯等）。
*   **对比基准（Baselines）**：
    *   **DreamerV2 / DreamingV2**：作为世界模型生成的基准。
    *   **DiWA**：作为下游策略学习的基准（该方法使用标准的 DreamerV2 特征进行扩散策略微调）。
*   **评估指标**：图像生成质量（PSNR, SSIM, LPIPS, FVD）以及任务成功率。

### 4. 资源与算力
*   **训练步数**：WAM 仅训练了 **230K 个梯度步**（约 51.2 万帧数据）。
*   **效率对比**：论文强调 WAM 的训练步数比基准模型（DreamingV2 的 2M 步）少了 **8.7 倍**。
*   **硬件说明**：文中**未明确说明**具体的 GPU 型号和数量，但提到了使用 AdamW 优化器及相关的超参数设置。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 8 个不同的操纵任务进行了独立评估。
    *   对比了生成质量（定量指标）和策略性能（成功率曲线）。
    *   每项任务评估了 29 个不同的初始配置。
*   **充分性与公平性**：实验设计较为公平。WAM 与 DiWA 使用了完全相同的策略架构（DiffusionMLP）和训练流程，唯一的变量是世界模型提供的特征。通过展示 BC 阶段和 PPO 微调阶段的性能提升，有力证明了特征质量的改进。

### 6. 主要结论与发现
*   **生成质量提升**：尽管训练步数更少，WAM 在所有视频预测指标（PSNR, SSIM 等）上均优于 DreamerV2，且生成的图像在长程预测中色彩更准确、形状畸变更小。
*   **策略性能飞跃**：
    *   **行为克隆（BC）**：平均成功率从 59.4% 提升至 **71.2%**。
    *   **PPO 微调**：平均成功率从 79.8% 提升至 **92.8%**，其中“打开灯泡”和“关闭 LED”两个任务达到了 100% 成功率。
*   **动作正则化的价值**：证明了在世界模型中显式建模动作预测能显著改善潜在空间的结构，使其更适合控制任务。

### 7. 优点（亮点）
*   **轻量且高效**：通过添加一个简单的 MLP 逆动力学头，在不增加显著计算负担的情况下，大幅缩短了世界模型的训练时间（8.7 倍加速）。
*   **即插即用**：不需要修改复杂的策略架构，仅通过改进特征表示就提升了现有 SOTA 方法（如 DiWA）的性能。
*   **理论完备性**：详细解释了动作信息如何从编码器级联到先验分布，为“为什么动作预测有助于想象”提供了逻辑支撑。

### 8. 不足与局限
*   **任务多样性限制**：虽然在 CALVIN 上表现优异，但实验仍局限于桌面操纵任务，尚未在更广阔的场景（如足式机器人或导航）中验证。
*   **超参数敏感性**：动作损失权重 $\lambda_{act}$ 被设为 1000.0，这表明模型可能对不同损失项之间的平衡比较敏感，跨领域迁移时可能需要精细调参。
*   **算力细节缺失**：未提供具体的训练时长和硬件消耗，难以精确评估其在实际工程中的部署成本。

（完）
